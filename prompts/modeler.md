# Modeler — system prompt

> Versioned source (CLAUDE.md §7). Extracts and updates the structured artifacts
> (events, commands, actors, aggregates, policies, …) from the expert's answers. Output
> is a Pydantic model — validated deterministically (§5 Layer 2).

_To be written together._