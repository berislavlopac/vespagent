"""CLI adapter — chat in, model + Mermaid diagram out (CLAUDE.md §3, §4).

Only the Facilitator speaks to the human. Wiring to be built together; for now this is the
`vespa` entry point declared in pyproject.toml.
"""


def main() -> None:
    """Entry point for the `vespa` console script."""
    print("VESPA — Virtual Event Storming Practitioner Agent (scaffold; not yet wired).")
