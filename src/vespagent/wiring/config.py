"""Configuration — typed settings (CLAUDE.md §4).

Pydantic-settings models for provider selection and runtime config. The default config
selects the LLM provider; the core neither knows nor cares which model answered. To be
built together.
"""
