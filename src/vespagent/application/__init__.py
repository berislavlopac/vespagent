"""Application layer — use cases / orchestration (CLAUDE.md §4).

Thin orchestration over the domain and the role ports. Framework-free; depends only on
domain types and ports, never on Pydantic AI or any adapter.
"""
