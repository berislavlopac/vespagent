"""Rendering adapters — implement the ModelRenderer port (CLAUDE.md §4).

Mermaid renderer first; the port stays stable so a web board can replace it without
touching the core. Live rendering is core to event storming, not decoration (§3).
"""
